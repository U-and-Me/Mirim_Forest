"use strict"

let box_color;

function sendMessage(){
    // 닉네임, 방명록 값 가져오기
    const name = document.getElementById('nick').value;
    const post = document.getElementById('guestbook').value;

    
}

function selectColor(){

}